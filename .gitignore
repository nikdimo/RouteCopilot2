# Learn more https://docs.github.com/en/get-started/getting-started-with-git/ignoring-files

# dependencies
node_modules/

# Expo
.expo/*
!.expo/web/
.expo/web/cache/
dist/
web-build/
expo-env.d.ts

# Native
.kotlin/
*.orig.*
*.jks
*.p8
*.p12
*.key
*.mobileprovision

# Metro
.metro-health-check*

# debug
npm-debug.*
yarn-debug.*
yarn-error.*

# macOS
.DS_Store
*.pem

# local env files
.env*.local

# VPS connection (contains host/key path)
connect-vps.bat

# typescript
*.tsbuildinfo

# generated native folders
/ios
/android

# Env files (secrets)
.env
.env.*

# Google Play service account key
google-service-account.json

# Temporary upload/cache folders (e.g. Drive upload)
.tmp.driveupload/

# EAS workaround scripts (not needed in build)
scripts/eas-build-pre-install.sh
scripts/ensure-expo-web-dir.js

# Editor/IDE and Windows reserved names (avoid "nul" etc. breaking git on Windows)
.claude/
Claude/
nul
con
prn
aux
